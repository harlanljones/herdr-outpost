[project]
name = "herdr-outpost-relay"
version = "1.0.0"
description = "Async daemon relay and gateway for herdr-outpost"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "websockets>=14.0",
    "pywebpush>=2.0.0",
    "py-vapid>=1.9.0",
    "python-telegram-bot>=21.0",
]

[project.urls]
Homepage = "https://github.com/harlanljones/herdr-outpost"
Documentation = "https://github.com/harlanljones/herdr-outpost#readme"
Issues = "https://github.com/harlanljones/herdr-outpost/issues"

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.hatch.build.targets.sdist]
include = [
    "*.py",
    "*.sh",
    "*.ps1",
    "README.md",
]
